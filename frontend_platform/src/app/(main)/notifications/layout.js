export const metadata = {
    title: 'Notifications',
    robots: {
        index: false,
        follow: false,
        googleBot: { index: false, follow: false },
    },
};

export default function NotificationsSectionLayout({ children }) {
    return children;
}
