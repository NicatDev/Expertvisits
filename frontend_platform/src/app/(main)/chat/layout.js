export const metadata = {
    title: 'Chat',
    robots: {
        index: false,
        follow: false,
        googleBot: { index: false, follow: false },
    },
};

export default function ChatSectionLayout({ children }) {
    return children;
}
